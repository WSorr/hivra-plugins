# Capsule Chat Test Plugin

Minimal external plugin scaffold for:

- `plugin_id`: `hivra.contract.capsule-chat.v1`
- `method`: `post_capsule_chat_message`

Current stage:

- deterministic wasm module stub only
- no transport execution in this repository
- package format is compatible with Hivra plugin installer flow

